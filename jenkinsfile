pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
		flyway info -url=jdbc:mysql://localhost:3306/moviesCI -user=root -password=Redg@te1
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}